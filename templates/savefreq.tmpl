#if $saveformat == 'xml'
#filter WebSafe
## XML
<?xml version="1.0" encoding="$corpencoding"?>

<frequency>
#if $heading
  <heading>
    <corpus>$corpname</corpus>
    <query>#slurp
    #for $d in $Desc
$d.get('arg')#slurp
    #end for
#slurp
</query>
  </heading>
#end if
  #for $b in $Blocks
  <block>
    <name>$b['blockname']</name>
    <items>
      #for $i in $b['Items']
      <item>
        #for $w in $i['Word']
	  <str>$w.n</str>
	#end for
	  <freq>$i.freq</freq>
	  #unless $ml
	  <rel>$i.rel</rel>
	  #end unless
      </item>
      #end for
    </items>
  </block>
  #end for
</frequency>

#end filter
#else
## text
#if $heading
\# Frequency list

\# Corpus: $corpname
  #for $d in $Desc
\# $d.get('op'):  $d.get('arg') $d.get('size')
  #end for

\# Frequency limit: $flimit

#end if
#for $b in $Blocks

  #for $h in $b['Head']
$h.get('n')	#slurp
  #end for

-------------------------------------------------------
  #for $i in $b['Items']
    #for $w in $i['Word']
$w.get('n')	#slurp
    #end for
$i.get('freq')	#slurp
    #unless $ml
$i.get('rel')
    #else

    #end unless
  #end for
#end for

#end if

